name: Infrastructure deployment

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment (devl or prod)'
        required: true
        default: 'devl'
        type: choice
        options:
          - devl
          - prod
      operation:
        description: 'Infrastructure deployment operation (plan, apply, or destroy)'
        required: true
        default: 'plan'
        type: choice
        options:
          - plan
          - apply
          - destroy

permissions:
  id-token: write
  contents: read

env:
  TF_IN_AUTOMATION: true
  TF_STATE_BUCKET: app-terraform-statefiles
  TF_STATE_KEY: ${{ github.event.inputs.environment }}/terraform.tfstate

jobs:
  Infrastructure:
    name: Deploy Infrastructure with Terraform
    if: ${{ github.event.inputs.operation != 'destroy' }}
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::139023234293:role/InfraDeployerRole
          aws-region: ap-southeast-2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.0

      - name: Terraform Init
        working-directory: terraform
        run: |
          terraform init -reconfigure \
            -backend-config="bucket=${{ env.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ env.TF_STATE_KEY }}" \
            -backend-config="region=ap-southeast-2"

      - name: Terraform Validate
        working-directory: terraform
        run: terraform validate

      - name: Terraform State Check
        working-directory: terraform
        run: terraform state list

      - name: Terraform Plan
        working-directory: terraform
        run: terraform plan -var="environment=${{ github.event.inputs.environment }}" -var="vpc_id=vpc-03ce369b7374fd1e6" -out=tfplan

      - name: Terraform Apply
        if: ${{ github.event.inputs.operation == 'apply' }}
        working-directory: terraform
        run: terraform apply -auto-approve -input=false tfplan

  Destroy:
    name: Destroy Infrastructure with Terraform
    if: ${{ github.event.inputs.operation == 'destroy' }}
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::139023234293:role/InfraDeployerRole
          aws-region: ap-southeast-2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.0

      - name: Terraform Init
        working-directory: terraform
        run: |
          terraform init -reconfigure \
            -backend-config="bucket=${{ env.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ env.TF_STATE_KEY }}" \
            -backend-config="region=ap-southeast-2"

      - name: Terraform State Check
        working-directory: terraform
        run: terraform state list

      - name: Terraform Destroy
        working-directory: terraform
        run: terraform destroy -auto-approve -input=false -var="environment=${{ github.event.inputs.environment }}" -var="vpc_id=vpc-03ce369b7374fd1e6"
